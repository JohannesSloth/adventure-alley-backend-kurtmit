DateTimeFormatter som skal bruges: DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm");
For at formattere tilbage til int: LocalDateTime.parse(xx.format(formatter),formatter);

